import styled from "styled-components";

const Footer = () => {
  return <Container>This is the Footer Page</Container>;
};

export default Footer;

const Container = styled.div`
  display: flex;
  background-color: red;
`;
